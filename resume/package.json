{
  "name": "resume",
  "version": "1.0.0",
  "description": "My resume",
  "main": "src/main.ts",
  "scripts": {
    "generate": "ts-node src/main.ts",
    "compile": "tectonic build/mario_jimenez_resume.tex",
    "build": "yarn generate && yarn compile",
    "rebuild": "pushd ../data && yarn build && popd && yarn build"
  },
  "author": "MarioJim",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "@types/node": "^16",
    "data": "*",
    "ts-node": "^10",
    "typescript": "^4"
  }
}
